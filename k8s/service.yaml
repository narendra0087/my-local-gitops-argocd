apiVersion: v1
kind: Service
metadata:
  name: local-node-service
  namespace: default
  labels:
    app: local-node-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: local-node-app
