apiVersion: v1
kind: Service
metadata:
  name: local-node-service
  namespace: default
  labels:
    app: local-node-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: local-node-app
